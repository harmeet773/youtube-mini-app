# TODO: Convert to Backend-Only Project

- [x] Update package.json: Remove "ejs" and "express-ejs-layouts" dependencies
- [x] Update server.js: Remove EJS-related imports and configurations
- [x] Update homeController.js: Change `index` and `about` functions to return JSON responses
- [x] Run npm install to update dependencies
- [x] Test the server to ensure routes return JSON
